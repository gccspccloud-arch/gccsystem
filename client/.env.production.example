# Copy to .env.production and fill in for local production builds.
# On Render Static Site, set VITE_API_BASE_URL in the dashboard's Environment tab instead.
VITE_API_BASE_URL=https://gcc-api.onrender.com/api
